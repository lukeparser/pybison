name: Build CI wheels

on:
  push:
    branches:
      - '*'
      - '!master'
      - '!main'

jobs:
  call-build-step:
    uses: lukeparser/pybison/.github/workflows/cibuildwheel.yml@latest
    with:
      fail-fast: false
      deploy: false
